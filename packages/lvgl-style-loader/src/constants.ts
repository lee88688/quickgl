import * as constants from '@quickgl/constants';

export const STATE_SELECTOR: Record<string, number> = constants.lvState;

export const PART_SELECTOR: Record<string, number> = constants.lvPart;

export const STYLE_PROP: Record<string, number> = constants.lvStyleProp;